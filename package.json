{
  "name": "jo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "npx cypress run",
    "run_this": "npx cypress run --spec ",
    "install": "npm instal --save-dev cypress",
    "init": "npm init",
    "help": "npx cypress"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "3.1.5"
  }
}
